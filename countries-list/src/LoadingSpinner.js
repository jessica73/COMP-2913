import React from 'react';
import logo from './logo.svg';

const LoadingSpinner = () => (
    <div>
      <img src={logo} className="LoadingSpinner" alt="logo" /> Loading...
    </div>
  );

export default LoadingSpinner;